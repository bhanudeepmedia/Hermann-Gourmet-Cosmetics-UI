<div class="testimonials-section">
  <div class="container">
    <div class="section-header-center">
      <h2>{{ section.settings.heading }}</h2>
      <p>{{ section.settings.subheading }}</p>
    </div>

    <div class="testimonials-slider" id="testimonialsSlider">
      {% for block in section.blocks %}
        <div class="testimonial-card">
          <div class="stars">★★★★★</div>
          <p class="testimonial-text">"{{ block.settings.quote }}"</p>
          <div class="testimonial-author">
            <strong>{{ block.settings.name }}</strong>
            {% if block.settings.location %}
              <span>{{ block.settings.location }}</span>
            {% endif %}
          </div>
        </div>
      {% endfor %}
    </div>

    <div class="slider-dots" id="sliderDots"></div>
  </div>
</div>

<style>
  .testimonials-section {
    padding: 6rem 5%;
    background: var(--color-background);
  }
  .testimonials-slider {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    gap: 2rem;
    padding: 2rem 0;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }
  .testimonials-slider::-webkit-scrollbar {
    display: none;
  }
  .testimonial-card {
    min-width: 350px;
    flex-shrink: 0;
    scroll-snap-align: center;
    background: white;
    padding: 3rem 2rem;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.08);
    text-align: center;
  }
  .stars {
    color: #F2C94C;
    font-size: 1.5rem;
    margin-bottom: 1.5rem;
  }
  .testimonial-text {
    font-size: 1.1rem;
    line-height: 1.7;
    margin-bottom: 2rem;
    font-style: italic;
    color: #333;
  }
  .testimonial-author strong {
    display: block;
    font-size: 1.1rem;
    margin-bottom: 0.3rem;
  }
  .testimonial-author span {
    color: #999;
    font-size: 0.9rem;
  }
  .slider-dots {
    display: flex;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 2rem;
  }
  .dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: #ddd;
    cursor: pointer;
    transition: background 0.3s;
  }
  .dot.active {
    background: var(--color-accent);
  }
  @media (max-width: 768px) {
    .testimonial-card {
      min-width: 280px;
    }
  }
</style>

<script>
  // Simple slider dots
  const slider = document.getElementById('testimonialsSlider');
  const dotsContainer = document.getElementById('sliderDots');
  const cards = slider.querySelectorAll('.testimonial-card');
  
  cards.forEach((_, index) => {
    const dot = document.createElement('div');
    dot.classList.add('dot');
    if (index === 0) dot.classList.add('active');
    dot.addEventListener('click', () => {
      slider.scrollTo({
        left: cards[index].offsetLeft - 20,
        behavior: 'smooth'
      });
    });
    dotsContainer.appendChild(dot);
  });

  slider.addEventListener('scroll', () => {
    const scrollPos = slider.scrollLeft;
    const dots = dotsContainer.querySelectorAll('.dot');
    cards.forEach((card, index) => {
      const cardPos = card.offsetLeft - 20;
      if (Math.abs(scrollPos - cardPos) < 50) {
        dots.forEach(d => d.classList.remove('active'));
        dots[index].classList.add('active');
      }
    });
  });
</script>

{% schema %}
{
  "name": "Testimonials Slider",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Love Letters From Our Family"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Hear what our customers have to say"
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        {
          "type": "textarea",
          "id": "quote",
          "label": "Quote",
          "default": "This cream changed my skin completely! It's like a warm hug in a jar."
        },
        {
          "type": "text",
          "id": "name",
          "label": "Customer Name",
          "default": "Maria K."
        },
        {
          "type": "text",
          "id": "location",
          "label": "Location",
          "default": "Athens, Greece"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Testimonials",
      "blocks": [
        {
          "type": "testimonial",
          "settings": {
            "quote": "This cream changed my skin completely! It's like a warm hug in a jar.",
            "name": "Maria K.",
            "location": "Athens, Greece"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "quote": "I've never used anything so natural and effective. My grandmother would approve!",
            "name": "Sofia P.",
            "location": "Cyprus"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "quote": "The lavender soap smells like heaven. I'm obsessed!",
            "name": "Elena M.",
            "location": "London, UK"
          }
        }
      ]
    }
  ]
}
{% endschema %}
